<%= render "shared/navbar" %>
<div class="container">
  <div class="mt-4">
		<h1 class="center">Add Recipe</h1>
		<div class="border-form mb-4">
				<%= render 'form', recipe: @recipe %>
		</div>
		<%= link_to 'Back', url_for(:back), class: "actions" %>
	</div>
</div>
<%= render "shared/footer" %>
